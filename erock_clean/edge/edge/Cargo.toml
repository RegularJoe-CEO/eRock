[package]
name = "erock-edge"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
erock = { path = ".." }  # Path dep fixes no matching/unresolved [so-3] [so-23]
